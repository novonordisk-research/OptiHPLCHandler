name: Release
run-name: Release workflow
on:
  push:
    branches:
      - main
jobs:
  lint-and-test:
    uses: ./.github/workflows/test_and_lint.yml
  release:
    name: Release
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - run: echo "Release"
